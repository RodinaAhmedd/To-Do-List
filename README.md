# To-Do-List
A simple and clean To Do List web application built using HTML, CSS, and JavaScript. This project allows users to add tasks, mark them as completed, and delete them easily through a user-friendly interface.

## 🚀 Features:

- Add new tasks
- Mark tasks as completed (with line-through style)
- Delete tasks
- Save tasks using Local Storage
- Clean and simple UI
- Responsive and easy to use

## Technologies Used:

HTML5
CSS3
JavaScript

## How It Works:

1- Type a task into the input field.
2- Click the Add button to add the task to the list.
3- The task is automatically saved in Local Storage.
4- Click the circle button to mark a task as completed.
5- Click the ❌ button to delete a task.
6- All changes are saved automatically and restored on page reload.

## 💡 Future Improvements:

- Edit existing tasks
- Add keyboard support (Enter key)
- Filter tasks (All / Completed / Active)
- Improve responsiveness for mobile devices
